spring:
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://106.14.210.31:3306/lighting_metal?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root  # 替换为你的MySQL用户名
    password: Zhangliang@520  # 替换为你的MySQL密码
  # Redis配置
#  redis:
#    host: localhost
#    port: 6379
#    password:  # 替换为你的Redis密码（无则留空）
#    database: 0
#    timeout: 5000ms

# 自定义配置（跨境相关）
cross-border:
  # 汇率更新间隔（分钟）
  exchange-rate-interval: 5
  # 默认结算币种（美元）
  default-currency: USD
  # 五金类海关编码前缀（示例）
  customs-code-prefix: 7308